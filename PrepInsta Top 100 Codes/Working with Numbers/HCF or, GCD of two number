#include<bits/stdc++.h>
using namespace std;
int main() {
	int tc; cin>>tc; while(tc--) {
		int num1, num2; cin>>num1>>num2;
		int HCF=1;
		for(int i=1;i<=min(num1, num2);i++) { //for(int i=min(num1,num2);i>=1;i--)
			if(num1%i==0 and num2%i==0) HCF=i;
		}
		cout<<HCF<<endl;
	}
	return 0;
}
