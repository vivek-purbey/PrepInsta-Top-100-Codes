#include <bits/stdc++.h>
using namespace std;
int main() {
	int tc; cin>>tc; while(tc--) {
		string s; cin>>s;
		char digit; cin>>digit;
		int count=0;
		for(int i=0;i<s.size();i++) {
			if(s[i]==digit) count++;
		}
		cout<<count<<endl;
	}
	return 0;
}
