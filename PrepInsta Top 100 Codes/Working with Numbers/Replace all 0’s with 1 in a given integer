#include <bits/stdc++.h>
using namespace std;
int main() {
	int tc; cin>>tc; while(tc--) {
		int num; cin>>num;
		string s=to_string(num);
		for(int i=0;i<s.size();i++) {
			if(s[i]=='0') {
				s[i]='1';
			}
		}
		cout<<s<<endl;
	}
	return 0;
}
