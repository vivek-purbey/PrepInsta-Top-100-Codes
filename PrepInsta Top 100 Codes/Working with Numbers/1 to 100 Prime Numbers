#include <bits/stdc++.h>
using namespace std;
int main() {
	for(int i=2;i<=100;i++) {
		int x=i; // check i prime or not
		int count=0;
		for(int x=2;x<i;x++) {
			if(i%x==0) {
				count++;
				break;
			}
		}
		if(count==0) cout<<i<<" ";
	}
	cout<<endl;
	return 0;
}
